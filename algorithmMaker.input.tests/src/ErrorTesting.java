import org.junit.Test;

public class ErrorTesting {
	@Test
	public void testBasicErrors() {
		//TODO: Put these back in with proper error checking
//		assertEquals(null, QuickParser.parseInput("Given x, Find y st a(y)"));
//		assertEquals(null, QuickParser.parseInput("given x; Find y st a(y)"));
	}
}
