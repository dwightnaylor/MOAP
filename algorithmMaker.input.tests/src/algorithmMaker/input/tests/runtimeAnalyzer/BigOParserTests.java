package algorithmMaker.input.tests.runtimeAnalyzer;

import static org.junit.Assert.assertEquals;

import org.junit.Test;

import runtimeAnalysis.BigO.*;

public class BigOParserTests {
	@Test
	public void testBasicBigOParses() {
		//TODO:MW: Uncomment these when the equals() methods are done.
//		assertEquals(BigOParser.parseExpression("a+b"), new AdditionExpression(new Variable("a"), new Variable("b")));
//		assertEquals(BigOParser.parseExpression("a-b"),
//				new SubtractionExpression(new Variable("a"), new Variable("b")));
//		assertEquals(BigOParser.parseExpression("a*b"),
//				new MultiplicationExpression(new Variable("a"), new Variable("b")));
//		assertEquals(BigOParser.parseExpression("a/b"), new DivisionExpression(new Variable("a"), new Variable("b")));
	}
}
